name: auto_reply_sync
description: In-app floating logger for Flutter — structured events, levels, search/filter, network capture, and a DevTools-style overlay.
version: 0.2.1
homepage: https://github.com/bookwarmdev/auto_reply_sync.git
repository: https://github.com/bookwarmdev/auto_reply_sync.git

environment:
  sdk: ">=3.0.5 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  draggable_fab: ^0.1.4
  dio: ^5.4.0
  http: ^1.2.0
  chopper: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
